//ant.importBuild 'build.xml'
//ant.importBuild 'master-build/master-build.xml'
//ant.importBuild 'master-build.xml'

apply plugin: 'java'

task hello <<{
  println 'Hello, world!'
}

task intro(dependsOn: hello)<<{
  println "I'm Gradle"
}

task show << {
  println "compile source dir is               " + compileJava
  println "compile destination dir is          " + relativePath(compileJava.destinationDir)
  println "processResources destination dir is " + relativePath(processResources.destinationDir)
}
